{
  "license": "UNLICENSED",
  "scripts": {
    "build": "run-p build:**",
    "build:app": "rollup -c",
    "build:lambda": "netlify-lambda build src/functions",
    "autobuild": "rollup -c -w",
    "start:dev": "run-p bootstrap:functions bootstrap:dev autobuild",
    "bootstrap:dev": "sirv public --single --dev",
    "bootstrap:functions": "netlify-lambda serve src/functions"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "dependencies": {
    "bulma": "^0.7.5",
    "rollup-plugin-postcss": "^2.0.3",
    "sirv-cli": "^0.4.4",
    "svelma": "^0.2.0"
  },
  "devDependencies": {
    "netlify-lambda": "^1.6.3",
    "npm-run-all": "^4.1.5",
    "rollup": "^1.12.0",
    "rollup-plugin-commonjs": "^10.0.0",
    "rollup-plugin-livereload": "^1.0.0",
    "rollup-plugin-node-resolve": "^5.2.0",
    "rollup-plugin-svelte": "^5.0.3",
    "rollup-plugin-terser": "^4.0.4",
    "svelte": "^3.0.0"
  }
}
